<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start OCR"
        android:id="@+id/ocr_button"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="rescanOCR"
        android:id="@+id/ocrRescanBtn"
        android:nestedScrollingEnabled="false"
        android:onClick="rescan"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Edit"
        android:id="@+id/ocrToEditBtn"
        android:layout_alignTop="@+id/ocrRescanBtn"
        android:layout_toRightOf="@+id/ocr_button"
        android:layout_toEndOf="@+id/ocr_button"
        android:onClick="edit" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imageView"
        android:layout_below="@+id/ocr_button"
        android:layout_centerHorizontal="true" />

</RelativeLayout>